const styles = (theme) => ({
  cardActions: {
    display: "flex",
    alignItems: "center",
    justifyContent: "flex-end"
  },
  modal: {
    top: '50%',
    left: '50%',
    width: 400,
    border: '2px solid red',
    outline: 'none',
    position: 'absolute',
  }
});

export default styles;
